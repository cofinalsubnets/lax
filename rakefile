$LOAD_PATH << File.expand_path('../lib', __FILE__)
require 'lax'
require 'rspec/core/rake_task'
#require 'pry'

RSpec::Core::RakeTask.new 'spec'
task(:lax) { Lax::DEFAULT_RUNNER.call Dir['./test/*'] }
task default: :lax

